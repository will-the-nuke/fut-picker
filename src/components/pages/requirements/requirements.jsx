import React, {Component} from 'react';

class Requirements extends Component {

    constructor() {
        this.state = {};
    };

    render() {
        return (
            <React.Fragment>
            </React.Fragment>
        );
    };
};

export default Requirements;
